server {
  listen {{ nginx_port }} default_server;
  listen [::]:{{ nginx_port }} default_server;
  root /var/www/html;
  index index.html;
  server_name {{ ansible_fqdn }};
  location / {
    try_files $uri $uri/ =404;
  }
}
